## DOCKER

> Rodar o docker
    - docker run -d --name mysql -e MYSQL_ROOT_PASSWORD=docker -p 3306:3306 mysql:latest

> Iniciar o container
    - docker start mysql

> Parar o container
    - docker stop mysql